# Synthetic sample

This folder contains a tiny, fully synthetic sample used by
`examples/walkthrough.md` when the full validation CSVs are not locally
available.

It is generated by `generate_sample.m` (to be added) and is intentionally
small (50 frames) so the walkthrough completes in a few seconds. No real
animal data is included here.
